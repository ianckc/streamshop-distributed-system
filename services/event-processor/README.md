# event-processor

Rust consumer for `order.created` events: ClickHouse facts + Postgres status.

See the **event-processor** page in the docs site (`cd docs && npm start` → Services → event-processor).
