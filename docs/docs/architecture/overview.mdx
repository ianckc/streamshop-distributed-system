---
sidebar_position: 1
---

# Architecture Overview

StreamShop is an event-driven commerce and analytics platform. The **target** architecture routes requests through an API gateway to polyglot microservices; async events flow through a Kafka-compatible queue into an OLAP store.

## What's running today

```mermaid
flowchart LR
  Client[Client] --> Traefik[Traefik]
  Client --> Catalog1[catalog-api-1]
  Client --> Catalog2[catalog-api-2]
  Client --> OrderAPI[order-api]
  Traefik --> Nginx[nginx]
  Traefik --> OrderAPI
  Nginx --> Catalog1
  Nginx --> Catalog2
  Catalog1 --> Mongo[(MongoDB_8)]
  Catalog2 --> Mongo
  Catalog1 --> Redis[(Redis_8)]
  Catalog2 --> Redis
  Catalog1 --> MinIO[(MinIO)]
  Catalog2 --> MinIO
  OrderAPI --> Postgres[(PostgreSQL_18)]
  Redpanda[Redpanda]
```

- **Traefik** on `:8080` — `/api/orders` → order-api, `/api/catalog` → nginx → catalog replicas; dashboard on `:8081`
- **nginx** — round_robin across `catalog-api-1` and `catalog-api-2`
- **catalog-api** — replica 1 on `:3001`, replica 2 on `:3011` (`GET /health`, `GET /ready`, product list/detail)
- **order-api** on `:3002` — `GET /health`, `GET /ready`, `POST /api/orders` (also reachable directly)
- **PostgreSQL** — `orders` and `order_items` tables
- **MongoDB** — `products` collection
- **Redis** — cache-aside for catalog list/detail (TTL 60s; fail-open)
- **Redis Insight** on `:5540` — local GUI; not behind Traefik
- **MinIO** — S3 API `:9000`, console `:9001`; buckets `product-images` and `exports`; catalog-api returns presigned `image_url` on product reads (upload via API is planned)
- **Redpanda** v26.2.1 — Kafka API `:19092` (host), `redpanda:9092` (Compose); no producer yet

Details: [catalog-api](../services/catalog-api), [order-api](../services/order-api), [Quick Start](../getting-started/quick-start).

## Target architecture

```mermaid
flowchart TB
  subgraph clients [Clients]
    Browser[Browser_or_curl]
    Docs[Docusaurus_docs]
  end

  subgraph edge [Edge]
    Traefik[Traefik_API_Gateway]
    NginxLB[Nginx_Load_Balancer]
  end

  subgraph services [Microservices]
    Catalog[Catalog_API_Node]
    Order[Order_API_Go]
    Analytics[Analytics_API_Python]
    Processor[Event_Processor_Rust]
  end

  subgraph messaging [Messaging]
    Redpanda[Redpanda_Kafka_API]
  end

  subgraph storage [Durable_Storage]
    Postgres[(PostgreSQL)]
    Mongo[(MongoDB)]
    ClickHouse[(ClickHouse)]
    MinIO[(MinIO_S3)]
    Redis[(Redis_Cache)]
  end

  subgraph observability [Observability]
    Prometheus[Prometheus]
    Grafana[Grafana]
    Jaeger[Jaeger]
  end

  Browser --> Traefik
  Docs --> Traefik
  Traefik --> NginxLB
  Traefik --> Catalog
  Traefik --> Order
  Traefik --> Analytics
  NginxLB --> Catalog
  Catalog --> Mongo
  Catalog --> Redis
  Catalog --> MinIO
  Order --> Postgres
  Order --> Redpanda
  Redpanda --> Processor
  Processor --> ClickHouse
  Processor --> Postgres
  Analytics --> ClickHouse
  Analytics --> Postgres
  services --> Jaeger
  services --> Prometheus
  Prometheus --> Grafana
```

## Orchestration (target)

Docker Compose with optional profiles:

- **`minimal`** — gateway, core services, Postgres, Redis *(not yet defined)*
- **`full`** — all databases, Redpanda, MinIO, observability *(not yet defined)*

Today: `docker compose up` runs Traefik, nginx, postgres, mongo, redis, redisinsight, minio, redpanda, order-api, catalog-api-1, and catalog-api-2.

## Service responsibilities (target)

| Service | Owns writes to | Reads from |
|---------|----------------|------------|
| `catalog-api` | MongoDB, MinIO | Redis (cache) |
| `order-api` | PostgreSQL | — |
| `event-processor` | ClickHouse, PostgreSQL (status) | Redpanda |
| `analytics-api` | — (read-only) | ClickHouse, PostgreSQL |

**catalog-api today:** two replicas behind nginx; `GET /health`, `GET /ready` (Mongo ping), `GET /api/catalog/products`, and `GET /api/catalog/products/:id` with Redis cache-aside and MinIO presigned `image_url`; upload via the API is not wired yet.

**order-api today:** writes to PostgreSQL only; exposes `GET /health`, `GET /ready` (Postgres ping), and `POST /api/orders` (no tracing or metrics yet).

## Component index

| Component | Technology | Doc | Status |
|-----------|------------|-----|--------|
| API Gateway | Traefik v3 | [API Gateway](./api-gateway) | **Built** (`/api/orders`, `/api/catalog`) |
| Load Balancer | nginx | [Load Balancing](./load-balancing) | **Built** (2× catalog-api) |
| Cache | Redis 8 | [Caching](./caching) | **Built** (cache-aside, 60s TTL) |
| Message Queue | Redpanda | [Messaging](./messaging) | **Built** (broker only; produce planned) |
| SQL (OLTP) | PostgreSQL 18 | [Storage](./storage) | **Built** |
| NoSQL | MongoDB 8 | [Storage](./storage) | **Built** |
| OLAP | ClickHouse 24 | [Storage](./storage) | Planned |
| Object storage | MinIO | [Storage](./storage) | **Built** (presigned GET on catalog reads) |
| Observability | OTel, Jaeger, Prometheus, Grafana | [Observability](./observability) | Planned |

## User journeys (target)

1. **Browse catalog** — cached product list behind nginx (2 replicas)
2. **Upload product image** — MinIO via catalog-api
3. **Place order** — Go service writes to Postgres, publishes `order.created` *(write only today)*
4. **Async processing** — Rust consumer → ClickHouse
5. **Analytics** — Python queries ClickHouse + Postgres

See [Order Placement](../data-flows/order-placement) for the full flow and what's implemented.
