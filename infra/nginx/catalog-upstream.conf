upstream catalog {
    # round_robin (nginx default) so sequential curls hit both replicas.
    # Use least_conn under concurrent load if you change this.
    server catalog-api-1:3001 max_fails=3 fail_timeout=30s;
    server catalog-api-2:3001 max_fails=3 fail_timeout=30s;
}
